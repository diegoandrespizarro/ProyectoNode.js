const mongoose = require("mongoose")

const ProductShema = new mongoose.Schema({
        name:




        
})

const ProductShema = mongoose.model("Product",CartShema)
mongoose.exports = ProductShema